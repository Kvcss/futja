name: futja_app
description: "Encontre a partida perfeita"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter

  # Ícones estilo iOS
  cupertino_icons: ^1.0.8

  # Firebase (core + autenticação)
  firebase_core:
  firebase_auth:

  # MVVM com ChangeNotifier
  provider:

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
